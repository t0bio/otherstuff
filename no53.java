import java.util.ArrayList;

class Solution {
    public int maxSubArray(int[] nums) {
        ArrayList<Integer> lis = new ArrayList<>();
        int largestsum = nums[0];
        for(int i=0;i<nums.length;i++){
            lis.add(index, element);
        }
    }
}

